export { default as variant } from './variables';

export { default as light } from './themes/light';
export { default as dark } from './themes/dark';

export interface CustomTheme {
  red: string;
}
